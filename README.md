# Simple OpenCV Motion Detector

To activate motion-detector mode press "d". Algorithm waits 5 seconds before intializing motion-detector mode. <br>
Upon motion detection it logs the datetime of the event and saves camera snapshot in snapshots folder. <br>
To go back to normal mode press "p", to quit press "q" or esc. 
